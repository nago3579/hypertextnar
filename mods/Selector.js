import React, { Component } from 'react';
import { Text, View, Platform, StyleSheet } from 'react-native';

class Selector extends Component{
  // const changePos = () => {
  //       props.handler(props.optPos);
  // },

  render() {
    <View>
      <Text //onClick= {changePos}>
      >
      {props.text}
      </Text>
    </View>
  }
};

export default Selector;
